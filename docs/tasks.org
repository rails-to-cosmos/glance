#+TITLE: glance — our tasks
#+TODO: TODO NEXT WAITING | DONE CANCELLED

Deferred work, one headline a task.  Close it with DONE when it lands.  Opened
during the MCP + doc-pane session (2026-09-08).

* DONE Sweep the Elm sources to CODESTYLE.md
Collapsed ~22 short multi-line =if= / =let= / =case= / top-level defs across
=Body.elm=, =Doc.elm=, =Listing.elm=, =Scan.elm=, and removed blank lines between
=let= bindings — −44 lines.  =make elm= gave a byte-identical =assets/elm.js= and
=elm-test= stayed 189/189, so every change is pure formatting.

OPEN convention call: argument-position =if=s (e.g. =f (if c then a else b)=) —
CODESTYLE Rule 1 does not exempt them, but the author keeps some expanded
(=viewCells=) and one-lines others (=Doc.elm:1938=).  This sweep collapsed the
short ones in =Body=/=Listing= and left =Doc.elm='s expanded; pick one and I
finish it uniformly.

* CANCELLED Fix the selected-row repaint artifact when a column is selected
See [[file:bugs/fixed/2026-09-08-a-selected-row-repaints-with-artifacts-when-a-column-is-selected.md]].
CANNOT REPRODUCE (2026-09-09): the reporter no longer sees it and no code touched
the widget, so the diagnosed CSS mechanism can't be the standing cause — likely a
transient GPU/compositing paint.  Reopen and re-diagnose if it returns.

* DONE Fix the one-frame row-highlight blink when =f= enters a table
See [[file:bugs/fixed/2026-09-09-entering-a-table-with-f-blinks-the-row-highlight-one-frame.md]].
=paintSelection= stamps the selection synchronously in =assets/table-view.js=;
regression test =the widget stamps a selection in the same tick=, full browser
suite 97/97.  Vendored copy only — see the reconcile task below.

* DONE Reconcile the table-view source with the vendored copy
The source =../table-view/web/table-view.js= carried the in-cell editor on
=origin= all along; the local checkout was just stale.  A rebase pulled it in and
replayed the blink-stamp fix on top, and =assets/table-view.js= now matches the
source byte for byte — =sync-renderer= is safe again.

* DONE MCP stage 3 — a daemon-aware =glance mcp=
See [[file:proposals/proposed/2026-09-08-the-commands-are-mcp-tools.md]].  =/status=
now carries the served =dir=; =glance mcp= probes it (=mcpDaemonAt=) and, when a
ready daemon on =--port= owns =--dir=, forwards stdin/stdout JSON-RPC to that
daemon's =/mcp= — one live store, no stale view — else boots an offline store.
Test =mcp proxies to a daemon that owns the tree, and probes safely otherwise=;
cabal 2277, AGENTS 0.

* TODO Review and commit the session's stack
MCP stage 1 + 2, the doc-pane batch (9 stale TestServe cases aligned to the
mount model, case 84 and 15 fixed, case 70 split into 5, the retry defect fixed,
the unset-planning colour bug), the Elm one-liners, and the repaint-bug file.
All four gates green: cabal 2276, elm 189, browser 96, AGENTS 0.  Nothing
committed yet.

* DONE Put the graph on the MCP wire
See [[file:proposals/done/2026-09-10-the-graph-an-agent-can-walk.md]].
=edges: true= carries each row's =refs= and =referrers= off one reverse index,
=neighbors= walks the subgraph inside the daemon, the rows shape was already
there, and =add-link= writes a body or title edge through the one write door.

* DONE Fix the path-id leak on a broken property drawer
See [[file:bugs/fixed/2026-09-10-a-broken-drawer-gives-a-row-a-path-id-the-other-routes-refuse.md]].
=salvagedIdentity= reads the =ORG_GLANCE_ID= line out of a drawer the parse
refused, so the row, the ledger key and =get-headline= all spell the uuid again;
=doctor= now lists the idless and the broken-drawer blobs by path.

* DONE The daemon tails org-glance's WAL
See [[file:proposals/done/2026-08-26-the-daemon-tails-the-wal.md]] and
[[file:bugs/fixed/2026-08-26-a-headline-org-glance-creates-is-invisible-until-restart.md]].
The one tree watch takes =.org-glance/meta='s own files beside the documents:
each record appended to =headlines.jsonl= nudges its blob's path through the one
queue door, so a headline org-glance captures into a FRESH shard no longer waits
for a restart.  The cursor carries the file it read and the offset into it, so a
seal — rename plus touch — starts the read over even when both segments are the
same length.  A =meta= minted under a running daemon is armed by its own
directory event, fsnotify arming a new directory without traversing into it.

* TODO An interop case for the WAL tail (needs the peer)
The proposal's own oracle: org-glance captures into a store the daemon is
already watching, and =/headlines= grows the row inside a bounded poll.  Left
out because =test/interop/= needs Emacs and a peer whose seed dies today
([[file:bugs/open/2026-08-20-interop-seed-dies-on-a-peer-api-that-never-landed.md]]),
and the case would want a new elisp action beside it.  The Haskell cases under
=The daemon tails org-glance's WAL= carry the law meanwhile.

* TODO Finish the two-writer relay: a hash on the note, a pending layer in the fold
See [[file:proposals/draft/2026-09-10-two-writers-one-wal.md]].
Direct dual append to =headlines.jsonl= is refused (PIPE_BUF, seal ownership,
=seq=, two parsers).  Instead: =hash= in =EXTERNAL.jsonl= lines, =foldSegments=
reads the note file as a provisional segment so =doctor= tells pending from
drift, and one interop parity case on =relations= vs =hrLinks= as sets.

* TODO Repair the live store's git handling of the notification family
See [[file:bugs/open/2026-09-10-the-live-store-tracks-external-jsonl-and-union-merges-every-jsonl.md]].
=EXTERNAL.jsonl= and =COMPLETIONS.jsonl= are tracked and =*.jsonl merge=union=
is the retired glob; a note folded on another host makes a record with no blob
(30 today).  One-time =git rm --cached= + two-predicate =.gitattributes=, then a
=doctor= check so it cannot silently recur.

* TODO Decide: the files are the log, the cache stays home
See [[file:proposals/draft/2026-09-10-the-files-are-the-log-and-the-cache-stays-home.md]].
Direction, not a patch: one git-ignored SQLite cache per machine, keyed by blob
hash, written by Emacs alone or by Emacs and the daemon (SQLite is the lock; the
daemon stays optional so org-glance still ships standalone); only blobs travel.
Retires =org-glance-graph.el= (1806 lines) and invariants 1/7/8/34.  The middle
stop is =two-writers-one-wal.md=; pick one before either lands.

* WAITING Review the git sync UI (branch =feat/git-sync=)
From =spikes/2026-09-10-git-sync/=, now wired into the daemon.  =GET /git= reads
the work tree (=git status --porcelain=v2=), =POST /git/sync= runs the one action
per state; =Glance.Web.Git= shells git in =soDir=, loopback only.  A control at
the end of table-view's chip strip (=80-git.js=): =@ ⎇ dir:branch= then a state
glyph — clean fetches, behind pulls, ahead pushes, dirty commits + pushes,
diverged syncs.  Model B (auto-sync) commits + pushes after each write, off the
request path, gated behind =git config glance.autosync= AND a UI arm (=⟳=).
Tests: =TestGit= (parse + one-step); manual browser probe.  NOT merged.
UPDATE 2026-09-10: the control was redesigned to a shell-owned row (=#ghead=,
above the table) after it kept vanishing on =g= — see
[[file:bugs/fixed/2026-09-10-the-git-control-vanishes-on-a-view-re-apply.md]].

* DONE A git-repo fixture for the browser suite, and a git-control case
=test/browser/drive.mjs= serves a copy of =test/browser/tree/= that is not a git
repo, so =GET /git= answers =repo:false= and the git control never mounts under
the suite — it is verified only by a CDP probe today.  =git init= the served
tree (or a dedicated fixture) so a case can assert the control shows, runs its
one-click action, and survives =g= (a re-mount).  Guard that the added =#ghead=
row does not perturb the position-sensitive table cases.

DONE 2026-09-12: a =repo: true= case is served a SECOND =git init='ed copy of the
tree (one commit, no remote, the repo's own identity), fresh per case — =#ghead=
costs 22.2px above the table, so every other position-sensitive case keeps the
plain tree.  Two cases: the control mounts reading =repo:main= and the same =#gitctl=
node survives =g=; a sheet write dirties the work tree and the glyph's hover text
names the count.  =BREAK=ghead-row= turns the first red.  100/100, four full runs.

UPDATE 2026-09-12: the glyph stopped being a button.  It is a =span= the state
colours, its =title= a sentence built from the =/git= fields (=main tracking
origin/main · 3 uncommitted (1 staged, 2 unstaged, 0 untracked) · 2 behind · 1
ahead=); only the auto-sync toggle takes a click.

* TODO Fold the three lazy list mounts in the glue onto one helper
=linksMounted= (=40-popups.js=), =tagsMounted= (=40-popups.js=) and
=statesMounted= (=50-settings.js=) are the same four lines: return the mount if
it stands, else =listing(...)= it.  A =lazyList(...args)= closure was tried and
DROPPED — the three call sites also read the raw, possibly-null =let= in
fourteen places (=mount: () => lmount=, =unnarrow(smount)=, and the
=!smount= guard at =50-settings.js:260= that must NOT force a mount), so a
closure that hides the variable either forces the mount or needs a second
accessor: longer, and the guard changes meaning.  Worth revisiting if =listing=
grows a handle that answers "mounted?" itself.

* TODO Three keydown preambles the =onKeys= helper cannot take
=onKeys= (=05-keys.js=) folded six of the nine document-level preambles.  Three
stay hand-rolled: =60-refer.js:229= and =50-settings.js:541= register on the
CAPTURE phase (=addEventListener(..., true)=), which =onKeys= does not offer;
=70-shell.js:287= clears =promptNow().raising= BEFORE the key is named, so the
mutation would have to live inside the predicate.  Give =onKeys= a capture flag
and the first two fold too.

* CANCELLED Fold the glue's =VERBED= table and =draftWrote= ladder into one =VERBS= table
MOOT 2026-09-12: =draftWrote= died with the sheet over a draft (capture-is-a-row
stage 6), so =VERBED= is the only table left and a twelfth command is one row.

* TODO Collapse the browser driver's =reseed= and =repoUp= into one =fixture(name, seed)=
=reseed= (=test/browser/drive.mjs:385-389=) and =repoUp= (=:396-409=) are the same
four moves — kill the daemon, re-copy =test/browser/tree/=, optionally seed, serve
on a fresh port — so a third fixture (a bare origin, a two-file tree) is a third
copy today.

* TODO A =driftTerms= list in =Data.Org.Index= so a third WAL drift term is one row
=state= and =archived= are spelled at six sites — =dfState=/=dfArchived=
(=Index.hs:247,248=), the two =length= folds (=:265,266=), =stateNote= (=:288=),
=archiveNote= (=:292=), the sample builder (=:280=) and the report line
(=:303,304=); one list of (name, reader, shown) rows would fold all six.

* TODO Decide whether =Data.Org.Index= splits, and weigh it against the files-are-the-log call
329 lines hold three things: the store's layout names (=metaDir=/=openSegment=/
=manifestFile=/=segmentNames=, =:46-101=), the fold over segments (=:110-186=) and a
POSIX tailer with its own =Fd= discipline (=:189-231=) — a =Data.Org.Tail= and the
layout names moving to Walk, unless
[[file:proposals/draft/2026-09-10-the-files-are-the-log-and-the-cache-stays-home.md]]
retires the fold altogether, which is the decision to take first.

* TODO Move the salvage reader beside the parser it repairs for
=salvagedIdentity=, =drawerRun= and =propertyLine= (=src/Data/Org/Types.hs:357-390=)
re-read a property drawer out of raw lines, sitting in =Types= below the parse
they exist because of; they belong beside =propertiesP= (=Parser.hs:238=) where the
drawer's shape is already spelled.

* TODO Enforce =DOCEDITS= membership so a new edit surface cannot be missed
=DOCEDITS = [DTITLE, DPARA, DPAIR, DDATE]= (=20-sheet.js:805=) is read by
=sheetOpen= (=:806=) and by three shut-everything loops (=:1550=, =:1930=, =:2226=);
a fifth edit surface added to the openers and not to the list stays open under a
close, and nothing says so — the popup-registry failure in a second object.

* TODO Join =rowSummaryPairs= and =viewColumns=, the two row shapes nothing compares
=rowSummaryPairs= (=Query.hs:2715-2722=) serves the MCP rows shape and =viewColumns=
(=:2573-2580=) the table's; both read the same =HeadlineRecord= fields, they
disagree on the tag key (=tags= against =tag=), and no test names them together.

* TODO A git fixture with a local bare origin, for the clean, ahead and behind glyphs
=repoUp= (=test/browser/drive.mjs:396-409=) makes a repo with NO REMOTE, so both
git cases can only read the warned =⚠= and a hover text saying =no upstream=; a
bare origin cloned beside it would let a case read =✓=, =↑=, =↓= and the
=tracking origin/main= half of the title for real.

* DONE Retire the draft-document half of =GET /capture= (capture-is-a-row stage 1)
See [[file:proposals/done/2026-09-12-capture-is-a-row-in-the-table.md]] stage 1.
DONE 2026-09-12 with stage 6: =GET /capture= answers ={cycle}= alone
(=captureView=, =src-web/Glance/Web/Routes.hs=), and
=draftJSON=/=draftCells=/=draftTagsCell=/=inheritedIn=/=inheritedTags= went with
it, as did =draftTemplate=/=draftPointLine=/=Inherited=/=draftSeeded= and their
exports in =Query.hs=.  =captureTemplateIn= and =bareTemplate= are now the
COMMAND's alone, so =Routes.hs= no longer imports them.  The capture command's
own road — =draftRecord=, =draftEntry=, =DraftCargo=, =draftKeywords=,
=draftStates=, =expandTemplate=, =templateParts= — is untouched and green.

* DONE Capture-is-a-row stage 3 — the keys inside the draft's cells
See [[file:proposals/done/2026-09-12-capture-is-a-row-in-the-table.md]] stage 3.
THE WALK LANDED, and with it the draft's way out.  The widget grew one mount
option, =onCellKey(e, {id, col, value})= (=assets/table-view.js:3960=, typedef
=:92=), asked at the head of the cell editor's keydown; =true= means the producer
took the key.
Over the draft alone (=draftKey=, =frontend/glue/35-draft.js:197=): =TAB= walks
the ring title → state → priority → tags and =S-TAB= walks it back, wrapping; the
closing cell's value goes into the phantom row before the next cell opens, since
=closeCellEditor= redraws; =ESC= drops the whole draft and leaves the close to the
widget, whose own reading of the key is exactly that.  =Keymap.hs= is unchanged —
an open cell stops every key it sees, so the draft's keys belong to the editor and
can be bound nowhere else.

=RET= was owed to stage 4 and is now the commit (below), so a draft is committed,
dropped, or standing on a refusal.  The state and priority cells offer a plain
text input; the value palette from =30-capture.js= is a later refinement, and the
seeded value is the input's initial text, so a cell walked through untouched keeps
what it held.

Four browser cases, red before the change: =TAB walks the draft's cells and wraps,
S-TAB walks them back=, =n and p in an open draft cell type rather than walk=,
=ESC drops the draft and leaves the rows byte for byte= (the proposal's oracle 6,
pulled forward with ESC) and =a landed row opens no cell editor, and the draft is
the only row that does=.  Suite 101/101.  ONE KEY PER CDP CALL in the walk case:
a fresh input takes focus a macrotask behind the press, so three TABs sent
together advance two cells.

The =draft= row field went in with FIVE readings, where the proposal named four:
the fifth is =placeDrafts= (=assets/table-view.js:2817=), which keeps a draft out
of the sort.  The main view declares a sort chain, and a draft's title is empty,
so the chain would park it in the blanks at the end rather than under the row the
reader opened it at.

* DONE Capture-is-a-row stages 4 and 5 — the commit, and the refusal in place
See [[file:proposals/done/2026-09-12-capture-is-a-row-in-the-table.md]] stages
4 and 5.  =RET= from ANY of the draft's cells folds the open editor's value into
the phantom and posts the whole capture through the ONE command that mints a blob
(=commitDraft=/=draftArgs=, =frontend/glue/35-draft.js:292=, =:274=).  The args
are the title, the DESTINATION as =tag=, the row's own run as =tags= and the two
scalars; no body, no drawer, no planning line, and no state the destination's
cycle lacks — =askCycle= already dropped that.  On =ok=: =arriving = a.id=, the
draft is spliced out, and the server's order is asked for at once
(=fetchRows(settled)=).

=arrived= (=frontend/glue/20-sheet.js:2106=) now HOLDS the id until the row is
visible rather than spending it on the next settle whatever it carries.
A command publishes nothing (=docs/invariants.md=, "No write route publishes its
own write") and the watch debounces 0.1 s, so the commit's own re-query answers
the store as it was BEFORE the write; spending =arriving= there left point where
it stood.  It is still dropped with the view it belonged to.

Stage 5: a refusal keeps the draft STANDING (=refuseDraft=, =:319=; the dress is
=dressDraft=, =:336=).  The word takes the hint cell's place beside the row —
=nothing to capture= for an empty
title, the server's own sentence for its 400 — the editor comes back to the title
with its text selected, and =g-refused= turns the dashed rule and the accent edge
=--g-warn= (=assets/page.css:1469=).  The next CONTENT keystroke clears both
(=contentKey=, =:207=); a walk and a movement leave them; only =ESC= dismisses.

Three browser cases, the first two red before the change: =RET from any cell
captures, and point follows the row the server places= (the inbox run asserting
the store, the table, point and =inbox.org=; the =tag:book state:NEXT priority:A=
run asserting the blob's headline is exactly =* [#A] zqbook :book:=, which reads
the letter, the run and the ABSENCE of the dropped keyword at once), =RET on an
empty title keeps the draft and says what it wants=, and the proposal's case 9,
=a state the destination's cycle lacks is dropped, and the hint says so=.  Suite
104/104.  THE ACCENT EDGE IS TRANSITIONED (=transition: box-shadow=, table-view's
own sheet), so the warn dress is waited out rather than read once — a reading
taken the moment the class lands still shows the colour it is moving off.

NOT DRIVABLE IN THE SHELL RIG: the POST body is observable there (=argsOf=,
=taggedOf=, =test/TestServe.hs=), but the rig's table stub opens no in-cell
=<input>= (=editCell= records =[id, col]= and nothing else,
=test/fixtures/shell-harness.js:669=), so =onCellKey= — the one dispatch a key
inside a cell reaches — is never called; and the fixture view declares only
=state= and =tag= columns, so the draft's title cell cannot open at all.  Driving
it would need a new act plus a =title= column in the fixture, which moves every
case that reads a column index.  A glue-source pin stands in
(=a draft commits at one RET, and a refusal keeps it standing=).

* DONE Capture-is-a-row stages 6 and 7 — the deletions, and the cases
See [[file:proposals/done/2026-09-12-capture-is-a-row-in-the-table.md]] stages
6 and 7.  THE FORM AND THE SHEET OVER A DRAFT ARE GONE.  =30-capture.js= kept
only the value palette and the link door and was =git mv='d to =30-palette.js=
(the one list is =Base.hs= =gluePartFiles=; =AGENTS.hs=, =frontend/jsconfig.json=
and =TestSelfContained= read it).  Out with them: the =#ktag=/=#klist= field and
its popup row (=Page.hs=, =Page/Popups.hs=, =AGENTS.hs= =surfaces=/=popTiers=),
=captureShape=, =draftIn=/=statesFor=/=tagsFor='s draft branches, and in
=20-sheet.js= =capturing()= with all ~20 branches, =showDraft=/=draftOf=/
=captureWhere=, =commitCapture=, =settleDraftPlan=/=typedPlan=/=dtyped=,
=captureSheet=/=capnote=, =bareCapture=/=bareDraft=/=tagsBeyond=/=bodyBelow=,
=openLanding=/=dlanding= and =draftWrote=/=drawCells= — the last of which the
=VERBED=/=draftWrote= fold task below was about, so that task is moot.
=70-shell.js= lost the =capture= surface and, with it, the =ghost= field no
other surface used.  =inherited=/=ONE_DAY= went from =35-draft.js=: a
filter-lent planning day is refused by the design.  ONE PORT KIND WENT WITH
=drawCells=: =Doc.elm='s =SetCells= had no sender left, which
=every port kind the shell sends is one an Elm program decodes= turned red —
=make elm= rebuilt =assets/elm.js=, elm-test 189.

Cases: =promptKeySpec= is now =Shell reschedule= — its capture half was the
form's, and the draft's own keys reach =onCellKey= alone, which the rig's table
stub never calls, so that half lives in =Shell draft row= and in the browser
suite.  Two drivable cases joined =draftRowSpec= (=+ raises no surface and
writes nothing=, =a second + leaves the one draft standing=); the =ktag:= step
and the =/capture= draft stub went from =test/fixtures/shell-harness.js=, whose
stub now answers a cycle per destination.  =captureViewSpec= pins ={cycle}=
alone.  Three glue-source pins lost their subjects and were re-pointed: the
wash lists (=#capture=), =cancelSheetEdit= (=bareCapture=) and the 16px field
guard (=#ktag=).  Browser: =the draft wears what the filter pins, and says
where it lands= added, the last of the proposal's ten.

* DONE Capture-is-a-row, the review pass — the hint, the priority, the walk
Three calls on the landed draft row (2026-09-12).  THE DESTINATION HINT IS GONE:
=draftHint=, the row's =hint= field and =dropped= went from =35-draft.js=, and
the widget's =Row=/=noteOf= now draw =refused= alone — the tags cell says where
the capture lands, and a dropped state is dropped silently.  THE PRIORITY CELL
WEARS ORG'S OWN SPELLING: =draftSeed= seeds =[#A]= the way every landed row's
cell reads (=priorityCell=, =Query.hs:2569=), =draftArgs= still sending the bare
letter through =priorityIn=.  THE WALK FOLLOWS THE HEADER: =draftWalk()= filters
=cols= by the four cells a draft owns, so =TAB= goes left to right in the order
the header draws — state, priority, title, tags in this table — and the title
still opens first.  Suite: cabal 2323, elm 189, browser 105.

* DONE Date-widget-in-the-cell stage 1 — the grammar moves to a shared part
See [[file:proposals/done/2026-09-12-the-date-widget-lives-in-the-cell.md]]
stage 1.  ONE GRAMMAR, ONE FILE: =frontend/glue/15-dates.js= is a new BARE
fragment holding what =20-sheet.js= carried at =:1087-:1403= and =:1021-:1031= —
the stamp regexes (=DATE=, =ACTIVE=, =INACTIVE=, =STAMP=, =STAMP_HEAD=), the
civil-day arithmetic, the month tables, the refusal sentences, the readers
(=verbatimDate=, =shiftBase=, =shippedDate=, =englishDay=/=englishLeft=/
=englishDate=, =wrappedDate=, =resolvedDate=, =readsDate=, =readsStamp=),
=dateNow=, the offer vocab, =dateWriting=, =dateGhost= and =dateStep=/
=dateStepped=.  No call site changed: the parts are one script scope and =15=
folds before =20=.

WRAPPED IT IS NOT.  The harness pins the reader's clock by REASSIGNING =dateNow=
(=dateon:=, =test/fixtures/shell-harness.js:1443=), so an IIFE alias would leave
the closure on its own binding and the pin would go quietly dead.
=TestSelfContained.wrappedWidgets= gains no row.  Proved by mutation: moving
=refDay= three days turned =the shifted arrows adjust in place= red by exactly
three days.

What STAYED in =20-sheet.js= and why: =dateAdjust= (reads =el("dwhen")=, =said=,
=dateMoved=), =dateOffers=/=drawGhost=/=fitCh=/=GHOST_CAP=/=editDay=/=readsWhen=
(the pane's own box), and =PLANNING=/=DATED=/=DRAWER_FRAME=/=IDENTITY_KEYS=/
=planningWord=/=pairRefused= (the drawer's).  Rosters: =Base.hs=, =AGENTS.hs=,
=frontend/jsconfig.json=, and =TestSpec='s title, now =the shell is eleven
parts=.  −328 from =20-sheet.js=, +335 in =15-dates.js=.  Gate: check-glue
clean, cabal 2323, elm 189, =runghc AGENTS.hs= 0, browser 105/105.

* DONE Date-widget-in-the-cell stages 3 and 4 — the cell's editor, and the draft's date stops
See [[file:proposals/done/2026-09-12-the-date-widget-lives-in-the-cell.md]]
stages 3 and 4.  =frontend/glue/36-date-cell.js= is a new BARE fragment (+153)
holding the in-cell editor both a landed row and a draft use: =DATE_CELLS= (the
two columns and the keyword each names), =dateCellDay= (the day stamped when the
editor ENTERS a cell, kept across a repaint), =dateCellNote= (the strip's line),
=dateCellKey=/=dateCellStep=/=commitDateCell=, the composed =cellKey=, and
=planKey=, which is the =C-c C-s= split.  The shipped mount gains
=editableKeys: DATE_KEYS=, =onCellInput: dateCellNote= and =onCellKey: cellKey=
(=00-core.js:211-216=).  Rosters: =Base.hs=, =AGENTS.hs=,
=frontend/jsconfig.json=, and =TestSpec='s title, now =the shell is twelve
parts=.  =assets/table-view.js= is UNTOUCHED, so the upstream list below stands
as it was.

THE =RET= SEAM IS THE HANDLER, never a binding: =HANDLERS.materializeRow=
(=70-shell.js:139=) reads the shipped column cursor and materializes only where
the column draws no date — the way =^= reads =state.selCol=.  =Keymap.hs= and
=AGENTS.hs='s =bindings= gain no row.

ONE EDITOR, TWO READINGS OF =RET=: =cellKey= asks =dateCellKey= first and
=draftKey= under it, so a date cell's STEP keys belong to every row while =RET=
over a STANDING row commits =set-planning= and a DRAFT's falls through to the
capture.  =DRAFT_CELLS= gains =scheduled= and =deadline= — the walk already
follows header order — and =draftArgs= gains =planning=, one entry per date cell
that holds something, carrying THE PHRASE.  No server change was owed:
=plannedEntry= already resolves =capture='s planning against the request's one
clock read.

Cases (browser, +8, 108 → 116): =RET on a SCHEDULED cell opens the date editor
and sends the phrase= (which reads the POST BODY, so the phrase travelling is
pinned rather than inferred from a stamp both roads would write), =the strip is
dark on the cell's own day=, =an unreadable date refuses in the cell=,
=S-<right> steps the ghost a day=, =ESC leaves the cell's date as it was=, =an
empty date clears the planning line=, =the draft's SCHEDULED stop lands the row
where the sort puts it=, =C-c C-s with nothing marked opens the cell at point=.
Three shipped cases moved with the ring: the draft's =TAB= walk, the capture
commit's comment, and the header-order assertion.  Gate: check-glue clean, cabal
2323, elm 189, =runghc AGENTS.hs= 0, browser 116/116 twice.

* DONE Date-widget-in-the-cell stage 5 — the keymap prose, README.org and the last cases
See [[file:proposals/done/2026-09-12-the-date-widget-lives-in-the-cell.md]]
stage 5.  The BEHAVIOUR of the split landed with stage 3 (=planKey=,
=36-date-cell.js=), so this is the prose and the filing.

README.org: the key table's =RET= row now reads /over a date cell, edit the
date/ (=:83=) and the two planning sequences have a row of their own (=:86=); a
bullet under the material document's own =C-c C-s= paragraph (=:146=) carries
the table half — the cell, the strip, its silence on the cell's own day, the
=S-arrows=, the marked/unmarked split and the phrase travelling.  THE CAPTURE
PARAGRAPH WAS STALE (=:103=): it still promised the =→ book= hint the review
pass deleted, and said nothing of the two date stops.

=docs/commands.md=: the date widget's own key table listed the pane's two keys
alone (=:127=).  It now carries the three table rows — =RET= on a date cell, the
two sequences with nothing marked, the two with rows marked — the strip's
silence, the unpainted stamp and the draft's =planning=, and /One widget, every
door/ names the four callers of the one argument spelling.

=CHANGELOG.md=: the date widget had three Unreleased bullets already; CAPTURE
IS A ROW HAD NONE — that whole arc (=7228806..b16b274=) never touched the
changelog — so it gained one.

=docs/invariants.md=: two entries, neither of which existed.  /The phrase
travels; the ghost is ink/ (=:303=) names the four surfaces that send the
field's own bytes and the one server read that resolves them, and is the rule a
client-side resolve would break on a day the two clocks agree — red nowhere.
/A draft is the renderer's own row/ (=:318=) is the capture-row laws: the open
editor, the row nothing else can reach, the byte-identical =ESC=, and the
=producer= placement that keeps a phantom out of the store's row list.
Re-anchored while there: =table-view.js:702= → =:743= (=dated=) and
=20-sheet.js:2160= → =:1693=, both carried off by the capture deletions and the
date move.

=AGENTS.hs:5358= gains the clause the proposal owed: the client's whole grammar
is =frontend/glue/15-dates.js=, which the pane and the cell both read.

Both proposals are =git mv='d to =proposals/done/= with =Status: done ·
2026-09-12= and a /Landed/ section — file:line per stage, and the departures:
the strip's silence is the PRODUCER's law rather than the widget's, the =RET=
seam is =HANDLERS.materializeRow= rather than a binding, and the draft's POST is
undrivable in the shell rig.  The capture-row one carries the review pass and
one more departure: =GET /capture= was RETIRED rather than narrowed — the cycle is
=GET /keywords?tag=NAME= (=Routes.hs:159=), the same question =?ids== answers
for a row that does not exist yet.

TWO ITEMS WERE ALREADY WHOLE.  The shell suite's ghost vectors needed no
re-pointing: they read =glueSource= (=TestServe.hs:11825=), every part
concatenated in =gluePartFiles= order, so no case spells a part's path and the
move cost them nothing.  And the last case, =C-c C-s with nothing marked opens
the cell at point= (=cases.mjs:6016=), already drives BOTH halves of the split —
the cell with none marked, the prompt with one.

=Keymap.hs:157= =planningHelp= stands: =C-c C-s= still takes "the marked rows,
or the row at point", =takesRows= is unchanged and only the surface splits.
Gate: =runghc AGENTS.hs= 0, =/Spec/= green.  No case touched, so no
=browser-check=.

* DONE The date cell is the pane's own box, laid over the cell
THE STRIP LOST ON REVIEW (2026-09-12, after seeing it live): it had no OFFERS —
the menu is =#dwoffer=, a box the pane owns, and a 118px cell had no room for it
— so completion was the one thing the cell lacked.  A date cell now opens THE
WIDGET THE MATERIAL DOCUMENT HAS, as an overlay.

=#ddate= moved OUT of =#mdoc= to the PAGE's root (=Page.hs:54=) and is
=position:fixed= (=page.css:752=): the sheet is =display:none= while the table
is up, so a box living in it could never stand over a row.  =placeEdit=
(=20-sheet.js:497=) gained two branches — =o.fixed= measures against the
VIEWPORT rather than the pane's padding box, and =o.over= puts the box IN THE
CELL'S OWN PLACE (its top, its left, its height), growing right past the cell's
edge with the cell's width as the floor and clamped off the viewport's edge.
THE BOX NEVER FLIPS, being the cell; only =#dwoffer= turns over, on
=#ddate.flipped= (=page.css:770=), where the window's foot leaves it no room —
the pick is =spikes/2026-09-12-date-overlay='s B.  =placeEdit= is now wired to
EVERY scroller in the capture phase rather than to =#mdoc='s alone, so the
mounted table's own scroll re-places the box.  =DDATE= is now the SURFACE'S NAME
alone (={box: "ddate"}=) and =editIn= matches on the box id, so =dateShape(o)=
can build a shape per open.  The door is
=openDateBox({rect, initial, today, b, foot, key, tight, onCommit, onCancel,
onWalk})= (=20-sheet.js:1080=); =dateKey= routes =RET= through the shipped wall
(=datePassed=) to =onCommit=, =dateTab= takes the offer then =onWalk=, and
=cancelDateBox= is =ESC= where the open owns it.  The listener's gate is now
=ddating() || (editing && ...)=: the box is the one surface that stands with no
document open.

Three callers, one path: the pane (=summonPlan=, =tight=, =dPlanAt=), a standing
row's cell (=openDateCell=, =36-date-cell.js:26=) and the draft's date stop
(=openDraftDate=, =35-draft.js:99=).  The widget owes two verbs for it —
=cellRect(id, col)= and =closeEditor()= — and LOSES =editableKeys=,
=onCellInput=, =refreshStrip= and the whole =tr.tv-strip= row with its sheet
(−97 lines).  =36-date-cell.js= went 115 → 61; =dateCellNote=, =dateCellKey=,
=dateCellStep=, =commitDateCell=, =cellKey= and =cellNote= are gone, the box's
own ghost and keys doing all of it.  The draft's refusal is the ECHO PILL's
(=said=) rather than a strip, the row keeping its warn dress.

Cases: five date-cell cases rewritten for the box, three new (=the offers
complete a month word in the cell's box=, =the draft's date stop opens the box,
TAB with no offer walks on=, =on the bottom row the offers flip above the box=),
five deleted with the strip and the rig that drove the two mount options.
Browser 119 → 117.  The flip case shrinks the log strip (=--g-logn=, the
settings sheet's own property) to one line first: at its default the chrome under
the table leaves 153px, which the 128px menu fits in, so the flip is unreachable
without it.  Three shell pins moved: the overlay's
=className=, the =display:none= family =#ddate= left, and =placeEdit='s origin.

* TODO A browser case writes a date and never puts it back, so a later case re-seeds
=a headlines answer arriving under a draft leaves it standing=
(=test/browser/cases.mjs=) fires =set-planning SCHEDULED 2026-12-01= at whichever
row the draft landed under — =drv-wide= in the shipped order — and restores
nothing.  =the draft's SCHEDULED stop lands the row where the sort puts it= then
opens on =-scheduled:*empty* sort:scheduled= and finds five rows where it names
three, so it goes red once and passes on the harness's re-seeded tree.  Both
full runs of 2026-09-12 carried that one retry.  The fix is the =putPlanning=
restore every other writing case already does, taken from a reading of the row
BEFORE the write since the row is chosen at run time; it costs two full
=browser-check= runs to prove and was left out of the date-overlay change.

* TODO A browser case for the frame tick's repaint, which needs a taller tree
=tick()= now redraws through =repaint()= (=assets/table-view.js=), so a window
that MOVED never wipes an open cell editor.  NOTHING DRIVES IT: =renderRows=
only repaints when the window moves, =first= leaves 0 only past =OVERSCAN= (15)
rows of scroll — 450px at =ROW_H= 30 — and the fixture's 29 rows give about
260px of scroll.  A case is owed either a fixture of ~60 headlines (which shifts
every position-sensitive case, so it wants a tree of its own the way =repo: true=
has one) or a viewport override in the driver.  The SIBLING defect is pinned:
=a store settle under an open draft leaves its date box standing=.

* TODO A tag on an inbox headline moves it under the tag (first tag wins)
See [[file:bugs/open/2026-09-12-a-tag-on-an-inbox-headline-leaves-it-in-the-inbox.md]].
=add-tag= edits the title line only; the tagged road (=captureBlob=) exists at
capture time alone. Two-file write through the one door: blob first, then the
inbox cut, ledger noted, both paths nudged. After the capture-row build.

* TODO A killed auto-sync leaves a stale index.lock; the readout says nothing
See [[file:bugs/open/2026-09-12-a-killed-auto-sync-leaves-a-stale-index-lock.md]].
Bracket each git step so shutdown never abandons a child; =gitStatus= reads a
stale lock as a state the glyph wears; never delete it from glance.

* TODO Upstream the producer row, =getEditing= and =onCellKey= to =../table-view= before the next sync-renderer
=assets/table-view.js= is documented above as byte-identical to
=../table-view/web/table-view.js= (see /Reconcile the table-view source with the
vendored copy/).  THIS BRANCH FORKS IT.  =make sync-renderer= copies the sibling
over the vendored copy, so run today it would clobber every change below without
a word.  Upstream them, then re-sync.

The widget changes, by name (line numbers are =assets/table-view.js='s on this
branch):

- =Row= gains the PRODUCER'S OWN ROW — ={producer, under, refused}= —
  replacing the glance-specific =draft= flag (typedef =:65=).
- =standing(r)= (=:3194=) is =!r.producer=, and is asked at =ordered()=
  (=:2934=), =placeProducers= (=:2921=, was =placeDrafts=), =markAll=,
  =selectStep=, =selectRow=, =setSelected=, =keepSelection= and
  =standingFrom= (=:3217=).
- =placeProducers= places by =under= rather than by the neighbour it was handed
  beside, on =setRows=, =upsertRow=, =deleteRow= and =applyDelta= alike; a
  delta's indices count standing rows alone.
- =ownRows()= (=:2196=) holds the producer's rows across a =setRows=, and
  =standing= also keeps them out of the filter's own vocabulary (=tagVocab=,
  =domainOf=) and out of a long press's and a double-click's default command.
- =refused= DRESSES a producer's row =tv-refused= (=rowClasses=) and draws
  nothing: =noteOf= and =noteCell= are GONE, with =rowText='s measurement of
  them and =rowHTML='s note cell.  What a row was refused for is the producer's
  to say where it says such things — one note mechanism, and no second slot.
- =holdEditor= / =resumeEditor= / =repaint= (=:4224=): a repaint holds the open
  input's value in the HANDLE and puts the caret back, never =select()=;
  =repaint(force)= takes the flag =renderRows= takes, and the FRAME TICK goes
  through it, so a window that moved never wipes an open editor.
- =getEditing()= on the handle (=:4147=, =:5463=), and =onCellKey='s cell
  carries the column =key= beside =col= (=:4111=).
- =openCellEditor= no longer closes for its callers; =editCell= takes an
  optional selection.
- The producer row's dress moved OUT of =assets/page.css= into the widget's own
  sheet as =tv-producer= / =tv-refused= (=:1799=), with a new =--tv-warn= token
  (=:1160=, paired with =--g-warn= in =Glance.Web.Theme=).
- =closeEditor()= on the handle: the open in-cell editor taken down writing
  nothing, for a producer that has moved the reader somewhere else.
- =cellRect(id, col)= on the handle: WHERE a cell is drawn, so a producer laying
  its own overlay over one asks the widget rather than walking its DOM.
- =OpenCell= (typedef): the cell a producer is asked about carries =raw= — the
  value the editor OPENED on — and =token=, a counter bumped per open, beside
  ={id, col, key, value}=.  It rides =onCellKey=.
- =holdEditor= / =resumeEditor= (=:4201=, =:4220=) hold EVERY row's editor the
  same way: the typed line rides in the handle's =value= and never enters
  =r.cells= — a standing row's cells being the store's and a producer's own
  cell being drawn by the input in it — and the re-open carries =raw= and
  =token= through =editCell='s fourth and fifth arguments, so a repaint is the
  SAME open rather than a new one.

RETIRED ON REVIEW (2026-09-12): =editableKeys=, =onCellInput=, =refreshStrip=
and the =tr.tv-strip= row were built for the date cell's reading and lost with
it — a date is edited in the document's own box laid over the cell, so no
standing row's cell opens an editor at all.  Nothing above was ever upstreamed,
so they are gone rather than deprecated.

* DONE TAB resolves the phrase in the date widget
User call (2026-09-13).  `TAB' inside the date box reads in ONE ORDER, and the
box's other keys are untouched: (1) an offer standing in the menu is taken,
exactly as it was; (2) with none left to take, a field that READS as a date is
rewritten to that reading's stamp -- the ghost's own ink, written where the
reader can edit it -- caret behind it, the ghost then silent because the field
is its own answer; (3) a field no reading takes is left alone, the =✗= already
said.  =S-TAB= is the walk back alone.

The surfaces split on the RING and nothing else: the pane's slot and a standing
row's cell have none, so the resolve is the whole press and the box stays; the
draft's date stop resolves AND walks on at the same press, so the cell shows the
concrete day and =capture='s =planning= carries that stamp -- no second =TAB=.

THE WIRE LAW IS UNTOUCHED (docs/invariants.md, "the phrase travels; the ghost is
ink"): =RET= still sends the field's own bytes, which after a =TAB= are a stamp
the server parses as a stamp.  The reading reaches the wire only through a key
the reader pressed and can see the answer of -- which is what =S-<arrow>='s step
has always done.

Code: =dateWrote= / =dateStepInto= / =dateResolveInto= in =15-dates.js:341-354=
(the step folded onto the shared writer), =dateTab= in =20-sheet.js:1105=.
Tests: shell =TAB resolves a readable phrase to its stamp and the ghost falls
silent=, =TAB on an unreadable phrase changes nothing=, =TAB with an offer
standing still takes the offer= (=TestServe.hs:5295=, =:5319=, =:5331=);
browser =TAB in a cell's date box resolves the phrase in place= (=:5561=) and
=a TAB-resolved draft stop captures the stamp it shows= (=:6011=), with the
draft's walk case now asserting the resolved stamp.  The shell helper =stepsTo=
was renamed =boxAfter=, its name having said "step" for a reading that is the
box's after any script.
