#+TITLE: glance — our tasks
#+TODO: TODO NEXT WAITING | DONE CANCELLED

Deferred work, one headline a task.  Close it with DONE when it lands.  Opened
during the MCP + doc-pane session (2026-09-08).

* TODO Sweep the Elm sources to CODESTYLE.md
Bring every =frontend/elm/src/*.elm= to [[file:../frontend/elm/CODESTYLE.md]]:
collapse short multi-line =if= / =let= / =case= and top-level defs to one line
where the whole thing fits (~100 columns) and each branch is one simple
expression — a literal, a variable, one application.  The rule already lives in
CODESTYLE.md; this is enforcement across the tree, not a new rule.

Cosmetic: the Elm compiler emits byte-identical =assets/elm.js=, so after
=make elm= confirm =git diff --stat assets/elm.js= is empty, per file.

Already collapsed in =Doc.elm=: the planning-slot separator and the =.dk dunset=
class (both were violations, one pre-existing, one from the unset-colour fix).

* CANCELLED Fix the selected-row repaint artifact when a column is selected
See [[file:bugs/fixed/2026-09-08-a-selected-row-repaints-with-artifacts-when-a-column-is-selected.md]].
CANNOT REPRODUCE (2026-09-09): the reporter no longer sees it and no code touched
the widget, so the diagnosed CSS mechanism can't be the standing cause — likely a
transient GPU/compositing paint.  Reopen and re-diagnose if it returns.

* DONE Fix the one-frame row-highlight blink when =f= enters a table
See [[file:bugs/fixed/2026-09-09-entering-a-table-with-f-blinks-the-row-highlight-one-frame.md]].
=paintSelection= stamps the selection synchronously in =assets/table-view.js=;
regression test =the widget stamps a selection in the same tick=, full browser
suite 97/97.  Vendored copy only — see the reconcile task below.

* TODO Reconcile the table-view source with the vendored copy — =sync-renderer= is unsafe
=assets/table-view.js= is AHEAD of the source =../table-view/web/table-view.js=:
the vendored copy carries the in-cell editor (=editCell=/=editHeader=/=onEdit=/
=.tv-cell-edit=, the 696d480 work) that the source LACKS.  So =make sync-renderer=
would REGRESS the widget (copy the older source over the newer asset).  The
=f=-enter blink fix (=paintSelection= stamps synchronously) is therefore in
=assets/table-view.js= only.  Back-port the in-cell editor AND the blink stamp to
=../table-view/web/table-view.js=, then =sync-renderer= is safe again.

* TODO MCP stage 3 — a daemon-aware =glance mcp=
See [[file:proposals/proposed/2026-09-08-the-commands-are-mcp-tools.md]].  Stage 1
(=POST /mcp=) and stage 2 (=glance mcp= stdio) are done and green.  Stage 3: when
=/status= says a daemon already owns the tree, proxy to its =/mcp= instead of
booting a second store — folds the two-writer stale-view risk away.

* TODO Review and commit the session's stack
MCP stage 1 + 2, the doc-pane batch (9 stale TestServe cases aligned to the
mount model, case 84 and 15 fixed, case 70 split into 5, the retry defect fixed,
the unset-planning colour bug), the Elm one-liners, and the repaint-bug file.
All four gates green: cabal 2276, elm 189, browser 96, AGENTS 0.  Nothing
committed yet.
