# The run targets' configuration (`make run', `make run-native',
# `make run-wasm').  Make reads this via `-include', so each line is a plain
# VAR=value; ~ expands in the recipes' shell.
GLANCE_DIR=~/sync/views
GLANCE_PORT=7777
