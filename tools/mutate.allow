# KNOWN-EQUIVALENT SITES, one per line, and each one is a claim a human made.
# TSV: file <TAB> line <TAB> rule <TAB> digest — the target's blob digest when the
# claim was made, first 12 chars, which is what makes an entry EXPIRE: edit the
# file and the digest moves, so the survivor is reported again.
# EQUIVALENT IS NOT SURVIVED — a survivor is a claim about the SUITE.  Putting a
# real hole here hides it.

# --- src-web/Glance/Web/Filter.hs @ 0d3f71693bfe, graded 2026-08-11 ---------
# `matchesFilter''s one-test arm is an OPTIMIZATION: `all ($ r) [test]' is
# `test r', and the second arm subsumes the first exactly.
src-web/Glance/Web/Filter.hs	197	arms	0d3f71693bfe
# THE `Order' ARM IS UNREACHABLE BY CONSTRUCTION and kept for TOTALITY: `compile'
# drops every view-key term before a test is built.  Verified by mutating both
# and running the whole suite green.
src-web/Glance/Web/Filter.hs	229	identity	0d3f71693bfe
src-web/Glance/Web/Filter.hs	255	bool	0d3f71693bfe
