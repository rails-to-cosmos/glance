#+TITLE: fixme
# Status is org's own checkbox: [ ] pending, [-] in progress, [X] done.
# SPC toggles one in glance's own material view.

1. [X] Theme colors spread across the repository, please implement a single source of truth file for each theme. Keep in mind that we will add new themes, so structure it properly.
   1. [X] Core: Glance.Web.Theme + one file per theme, both --g-* and --tv-* emitted from it; renderer palette is a :where() default so the page wins.
   2. [X] Theme should also determine TODO colors, because it determines row highlighting already which could conflict with TODO colors.
   3. [X] User could define TODO states, in that case he could set the color for each theme (in theme settings). — #+GLANCE_STATE_COLORS, edited per theme in the settings sheet's theme tab.
   4. [X] Put theme settings under the global settings for now (","). Maybe we are ready to split this global settings popup into tabs? — tabbed: general | theme | keywords, one panel at a time.
2. [X] In table-view.js when you specify styles, display each key on separate line like here: /home/akatovda/sync/stuff/glance/assets/table-view.js:1118 — whole sheet reformatted upstream (table-view cc9dd70) and synced.
3. [X] In "general settings", alongside with "default view" add "agenda view", use it to customize agenda view filter. Better: make a dropdown to customize view with 2 options: default, agenda.
4. [X] "q" in popups should close the popup. "q" in native window should exit the app (if user is on the main page).
5. [X] material: when updating TODO state from empty state it doesn't visually update in material view. — fixed by the stale-reload drop (df93761 line); verified live and pinned by a regression case.
6. [X] Big one: let's work on a proposal for recurring tasks. We need to take inspiration from org-clock, but implement it differently from the storage perspective. — docs/proposal-recurring-tasks.md
7. [X] UX: if current sorting is specified, when user does "/" → "sort:" → RET, this should autocomplete to the current sorting value. Then user could DEL some parts or add (via → or ,).
8. [X] UX refactor: each reusable widget in a separate file. — docs/proposal-widget-files.md (proposed; the refactor itself is staged there)
9. [X] material: headline inconsistency. Main headline supports state modification, tag modification, but underlying headlines don't. Here we need a deep dive to make the semantics truly recursive. Maybe treat child headlines as a "reference" with type "child_of"? Need to think about it in a separate proposal. — docs/proposal-recursive-headlines.md
10. [X] capture: if user calls capture from table with enabled tag filter, set this tag to capture headline template by default (if not specified in capture template itself).
11. [X] capture: need to re-implement this carefully, currently almost unusable. Let's discuss in a separate proposal. — docs/proposal-capture-elements.md
12. [X] capture: actually org capture template language has symmetry that you missed: it has https://orgmode.org/manual/Template-elements.html elements that we can model and use as widgets (as inputs for capture templates and more). — same proposal, docs/proposal-capture-elements.md
13. [X] native: filter differs from web version: default view lacks "sorting" badge. — cause: both project files wrote ONE binary path, so `make run' and `make run-native' overwrote each other and the window served whichever glue was embedded last. The flagged build has its own --builddir now. UP: in web version the same bug sometimes, "sorting" badge doesn't appear when using "g" kbd (default filter).
14. [X] search: let's define the search string format to be ALWAYS "{key}:{value}". For custom substrings, use "substring:{value}" (system should add "substring" key by default, user just specifies value. — substring: lands on both sides; a bare word stays the same spelling with the key elided.
15. [ ] Every popup window should have its unique url to share: "global settings" must update the page url, and every header should be reachable by a fragment. Example: http://127.0.0.1:7777/?q=state%3A*active*&page=settings#theme — invariant recorded in CLAUDE.md, enforcement pending the widget split.
16. [ ] search: the filter badge must SHOW "substring:test" when a bare word is applied, and "/" completion should offer "substring:" — CLAUDE.md already claims the grammar is key:value throughout, so the renderer owes the canonical spelling. With that, the widget can require a match.
