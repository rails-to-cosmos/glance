#+TITLE: fixme
# Status is org's own checkbox: [ ] pending, [-] in progress, [X] done.
# SPC toggles one in glance's own material view.

1. [-] Theme colors spread across the repository, please implement a single source of truth file for each theme. Keep in mind that we will add new themes, so structure it properly.
   1. [X] Core: Glance.Web.Theme + one file per theme, both --g-* and --tv-* emitted from it; renderer palette is a :where() default so the page wins.
   2. [X] Theme should also determine TODO colors, because it determines row highlighting already which could conflict with TODO colors.
   3. [-] User could define TODO states, in that case he could set the color for each theme (in theme settings). — config pragma #+GLANCE_STATE_COLORS lands; the settings-sheet UI rides with 1.4.
   4. [ ] Put theme settings under the global settings for now (","). Maybe we are ready to split this global settings popup into tabs?
2. [-] In table-view.js when you specify styles, display each key on separate line like here: /home/akatovda/sync/stuff/glance/assets/table-view.js:1118
3. [ ] In "general settings", alongside with "default view" add "agenda view", use it to customize agenda view filter. Better: make a dropdown to customize view with 2 options: default, agenda.
4. [ ] "q" in popups should close the popup. "q" in native window should exit the app (if user is on the main page).
5. [ ] material: when updating TODO state from empty state it doesn't visually update in material view.
6. [ ] Big one: let's work on a proposal for recurring tasks. We need to take inspiration from org-clock, but implement it differently from the storage perspective.
7. [ ] UX: if current sorting is specified, when user does "/" → "sort:" → RET, this should autocomplete to the current sorting value. Then user could DEL some parts or add (via → or ,).
8. [ ] UX refactor: each reusable widget in a separate file.
9. [ ] material: headline inconsistency. Main headline supports state modification, tag modification, but underlying headlines don't. Here we need a deep dive to make the semantics truly recursive. Maybe treat child headlines as a "reference" with type "child_of"? Need to think about it in a separate proposal.
10. [ ] capture: if user calls capture from table with enabled tag filter, set this tag to capture headline template by default (if not specified in capture template itself).
11. [ ] capture: need to re-implement this carefully, currently almost unusable. Let's discuss in a separate proposal.
12. [ ] capture: actually org capture template language has symmetry that you missed: it has https://orgmode.org/manual/Template-elements.html elements that we can model and use as widgets (as inputs for capture templates and more).
13. [ ] native: filter differs from web version: default view lacks "sorting" badge. UP: in web version the same bug sometimes, "sorting" badge doesn't appear when using "g" kbd (default filter).
14. [ ] search: let's define the search string format to be ALWAYS "{key}:{value}". For custom substrings, use "substring:{value}" (system should add "substring" key by default, user just specifies value.
